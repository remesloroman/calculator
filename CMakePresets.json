{
    "version": 3,
    "configurePresets": [
        {
            "name": "default-release-config",
            "description": "Release config with default compiler and Unix Makefiles build system",
            "generator": "Unix Makefiles",
            "binaryDir": "build/${presetName}",
            "cacheVariables": {
                "CMAKE_BUILD_TYPE": "Release"
            }
        },
        {
            "name": "default-debug-config",
            "description": "Debug config with default compiler and Unix Makefiles build system",
            "generator": "Unix Makefiles",
            "binaryDir": "build/${presetName}",
            "cacheVariables": {
                "CMAKE_BUILD_TYPE": "Debug"
            }
        }
    ],
    "buildPresets": [
        {
            "name": "default-release-build",
            "description": "Build preset that inherits from default-release-config",
            "configurePreset": "default-release-config",
            "targets": [
                "calculator"
            ],
            "cleanFirst": true,
            "verbose": false,
            "jobs": 8
        },
        {
            "name": "default-debug-build",
            "description": "Build preset that inherits from default-debug-config",
            "configurePreset": "default-debug-config",
            "targets": [
                "all"
            ],
            "cleanFirst": false,
            "verbose": true,
            "jobs": 8
        }
    ]
}